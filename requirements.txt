# Halcyon Credit — runtime + dev dependencies (Python 3.11+)
# Pinned with lower bounds; lock exact versions in deployment.

# ---- Agent orchestration ----
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# ---- LLM gateway ----
litellm>=1.50.0

# ---- API layer ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ---- Policy RAG ----
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ---- Risk model + explainability ----
xgboost>=2.1.0
lightgbm>=4.5.0
scikit-learn>=1.5.0
shap>=0.46.0
pandas>=2.2.0
numpy>=1.26.0

# ---- Evaluation ----
ragas>=0.2.0
dspy-ai>=2.5.0

# ---- Core / validation / config ----
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
structlog>=24.4.0
httpx>=0.27.0

# ---- Dev / quality (kept here for a single-file install; split to requirements-dev.txt later) ----
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.6.0
mypy>=1.11.0
pip-audit>=2.7.0
